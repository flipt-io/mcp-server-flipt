{
  "name": "mcp-server-flipt-monorepo",
  "private": true,
  "description": "Model Context Protocol servers for Flipt (v1 and v2)",
  "author": "Flipt Devs <dev@flipt.io>",
  "homepage": "https://flipt.io",
  "repository": {
    "type": "git",
    "url": "https://github.com/flipt-io/mcp-server-flipt.git"
  },
  "bugs": {
    "url": "https://github.com/flipt-io/mcp-server-flipt/issues"
  },
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.13.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "lint": "eslint 'packages/*/src/**/*.ts'",
    "lint:fix": "eslint 'packages/*/src/**/*.ts' --fix",
    "fmt": "prettier --write 'packages/*/src/**/*.ts' --ignore-path .prettierignore",
    "fmt:check": "prettier --check 'packages/*/src/**/*.ts' --ignore-path .prettierignore"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "eslint": "^9.22.0",
    "eslint-config-prettier": "^10.1.1",
    "eslint-plugin-prettier": "^5.2.3",
    "prettier": "^3.5.3",
    "typescript": "^5.3.3",
    "typescript-eslint": "^8.26.1"
  }
}
